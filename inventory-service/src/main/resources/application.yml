spring:
  application:
    name: INVENTORY-SERVICE
  config:
    import: optional:configserver:${SPRING_CLOUD_CONFIG_URI:http://localhost:8088}
  main:
    banner-mode: off

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}